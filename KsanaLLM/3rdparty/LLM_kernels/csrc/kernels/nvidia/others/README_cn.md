本目录用于维护从其他框架引入的算子。这些算子因为不能直接以仓库引用的方式引用，需要复制代码后，进行拷贝，删减和修改。
相关操作应尽量遵循以下规则：
1. 第一层是来源项目名，第二层目录为main，dev两个子目录。
   - main表示现在主要使用的算子，依赖相对稳定的第三方框架分支。
   - 当需要引入的一些算子与main的代码差异过大，不能直接升级时。可以开启dev目录。dev表示新引入的算子，可能依赖较新的第三方框架分支。
   - dev分支出现后，上层对main分支上依赖的算子应该尽快切换到dev上。当对于main分支的算子依赖切断后，删除main目录，dev目录的名字被修改为main。
2. 算子迁移应该尽量保持代码独立编译和最小化的原则，避免不必要的编译冲突风险。
3. main/dev的子目录结构尽量与第三方框架的结构对应，减少对比查看的成本。
